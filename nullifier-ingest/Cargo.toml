[workspace]
resolver = "2"
members = [
    "imt-tree",
    "nf-server",
    "pir-client",
    "pir-export",
    "pir-server",
    "pir-test",
    "pir-types",
    "service",
]

[profile.release]
lto = "fat"
codegen-units = 1
